
package Controller;

import Model.ConexionDB;
import Model.Usuario;


public class UserController {
/*
}
    public boolean guardarUsuario(Usuario usuario){
       /* connection conn = null;
        PreparedStatement stmt = null;
    
        ConexionDB conexionbasedatos = new ConexionDB();
        
        try {
            conn = Conexion.getConexion();
            String sql = "INSERT INTO ususarios (id_usuario, usuario, contraseña, nombre, rol, correo) VALUES (?, ?, ?, ?, ?, ? )"
            PreparedStatement ps = con.prepareStament(sql);
            ps.setString(1, usuario.getid_usuario());
            ps.setString(2, usuario.getusuario());
            ps.setString(3, usuario.getcontraseña());
            ps.setString(4, usuario.getnombre());
            ps.setString(5, usuario.getrol());
            ps.setString(6, usuario.getcorreo());
            
            int resultado = stmt.executeUpdate();
            
            return resultado > 0;
                    
              } catch (Exception e) {
        }
        
        
    }*/
}
